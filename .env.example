# 智谱AI账号配置
BIGMODEL_USERNAME=your_username_or_email
BIGMODEL_PASSWORD=your_password

# 可选：默认套餐类型 (lite | pro | max)
DEFAULT_PLAN=pro

# 可选：默认订阅周期 (monthly | quarterly | yearly)
DEFAULT_CYCLE=quarterly
